<UserControl
    x:Class="InkPoc.Controls.InkControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    x:Name="inkControl"
    d:DesignWidth="400">
    <UserControl.Resources>
        <Style x:Key="InkToolBarActionButton" TargetType="Button">
            <Setter Property="MinWidth" Value="{ThemeResource InkToolbarButtonWidth}"/>
            <Setter Property="MinHeight" Value="{ThemeResource InkToolbarButtonHeight}"/>
            <Setter Property="MaxHeight" Value="{ThemeResource InkToolbarButtonHeight}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="{ThemeResource InkToolbarButtonBackgroundThemeBrush}"/>
            <Setter Property="Foreground" Value="{ThemeResource InkToolbarButtonForegroundThemeBrush}"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <InkToolbar x:Name="toolbar" TargetInkCanvas="{x:Bind inkCanvas}" Visibility="{Binding ShowToolbar, ElementName=inkControl}">

                <InkToolbarCustomToolButton 
                Click="Selection_Click" 
                ToolTipService.ToolTip="Selection tool">
                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xEF20;" />
                </InkToolbarCustomToolButton>

            </InkToolbar>
            <Button Click="Undo_Click" Style="{StaticResource InkToolBarActionButton}" ToolTipService.ToolTip="Undo" Visibility="{Binding ShowUndoRedo, ElementName=inkControl}">
                <SymbolIcon Symbol="Undo"/>
            </Button>
            <Button Click="Redo_Click" Style="{StaticResource InkToolBarActionButton}" ToolTipService.ToolTip="Redo" Visibility="{Binding ShowUndoRedo, ElementName=inkControl}">
                <SymbolIcon Symbol="Redo"/>
            </Button>
            <Button Click="Clear_Click" Style="{StaticResource InkToolBarActionButton}" ToolTipService.ToolTip="Delete all" Visibility="{Binding ShowClearAll, ElementName=inkControl}">
                <SymbolIcon Symbol="Delete"/>
            </Button>
            <Button Click="openFile_Click" Style="{StaticResource InkToolBarActionButton}" ToolTipService.ToolTip="Open file" Visibility="{Binding ShowLoadFile, ElementName=inkControl}">
                <SymbolIcon Symbol="OpenFile"/>
            </Button>
            <Button Click="SaveFile_Click" Style="{StaticResource InkToolBarActionButton}" ToolTipService.ToolTip="Save file" Visibility="{Binding ShowSaveFile, ElementName=inkControl}">
                <SymbolIcon Symbol="Save"/>
            </Button>


            <Button Click="Cut_Click" Style="{StaticResource InkToolBarActionButton}" ToolTipService.ToolTip="Cut">
                <SymbolIcon Symbol="Cut"/>
            </Button>

            <Button Click="Copy_Click" Style="{StaticResource InkToolBarActionButton}" ToolTipService.ToolTip="Copy">
                <SymbolIcon Symbol="Copy"/>
            </Button>

            <Button Click="Paste_Click" Style="{StaticResource InkToolBarActionButton}" ToolTipService.ToolTip="Paste">
                <SymbolIcon Symbol="Paste"/>
            </Button>

        </StackPanel>

        <Border Grid.Row="1" BorderBrush="#000000" BorderThickness="1" Background="#FFFFFF" Margin="10">
            <Grid>
                <!-- Canvas for displaying selection UI. -->
                <Canvas x:Name="selectionCanvas"/>
                <!-- Canvas for displaying ink. -->
                <InkCanvas x:Name="inkCanvas"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
