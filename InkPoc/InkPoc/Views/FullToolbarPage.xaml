<Page
    x:Class="InkPoc.Views.FullToolbarPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Style="{StaticResource PageStyle}"
    xmlns:controls="using:InkPoc.Controls"
    mc:Ignorable="d">
    <Grid
        x:Name="ContentArea"
        Margin="{StaticResource MediumLeftRightMargin}">
        <Grid Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}">
            <controls:InkControl
                ShowToolbar="{Binding ElementName=showToolbarCheck, Path=IsChecked, Mode=TwoWay}"
                ShowSelectionTool="Visible"
                ShowCopyPaste="Visible"
                ShowOpenSaveFile="Visible"
                ShowUndoRedo="Visible"
                ShowClearAll="Visible" />
            
            <CheckBox x:Name="showToolbarCheck" IsChecked="True" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,15,20,0">Show Toolbar</CheckBox>
        </Grid>
    </Grid>
</Page>
