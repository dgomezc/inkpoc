<Page
    x:Class="InkPoc.Views.SmartCanvasPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:InkPoc.Controls"
    Style="{StaticResource PageStyle}"
    mc:Ignorable="d">
    <Grid
        x:Name="ContentArea"
        Margin="{StaticResource MediumLeftRightMargin}">
        <Grid
            Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <controls:InkControl
                Strokes="{x:Bind ViewModel.Strokes}"
                 ShowRecognize="True"
                 ShowEnableTouchInking="True"
                 ShowClearAll="True"/>
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
                <Button Content="Recognize Text" Command="{x:Bind ViewModel.RecognizeTextCommand}" />
                <TextBlock VerticalAlignment="Center" Margin="10,0" Text="{x:Bind ViewModel.RecognizeText, Mode=TwoWay}" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
